spring.application.name=currency-exchange-service
server.port=8000
eureka.client.service-url.default-zone=http://localhost:8761/eureka

## Spring DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://rafs.sch.ng:3306/rafsedun_academy?useSSL=false&useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
spring.datasource.username=rafsedun_user
spring.datasource.password=Iam2focused
# Initialize the datasource with available DDL and DML scripts
spring.datasource.initialization-mode=always


## Hibernate Properties

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.generate-ddl=true
spring.jpa.database=mysql
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.org.hibernate.envers.global_with_modified_flag=true
spring.jpa.properties.hibernate.envers.autoRegisterListeners=false
spring.jpa.hibernate.ddl-auto=create-drop
## Hibernate Logging

# Static resources reloading during development
spring.resources.cache.period=0

## Hibernate Logging
logging.level.org.hibernate.SQL=info
